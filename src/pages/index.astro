---
import Base from '@/layouts/Base.astro';
import Section from '@/components/ui/Section.astro';
import Logo from '@/components/Logo.astro';
import {
  ArrowDown,
  Calculator,
  Phone,
  ArrowRight,
  CheckCircle,
  MapPin,
  Clock,
  Star,
  Sparkles,
  Zap,
  Globe,
  HandHeart,
  Eye,
  DollarSign,
  Briefcase,
  Target,
  Award,
  TrendingUp,
  ChevronDown,
  Play,
  Building,
  Home,
  Users
} from 'lucide-react';
import { siteConfig } from '@/lib/utils';

const phoneLink = siteConfig.contact.phone.replace(/\s/g, '');

const websiteSchema = {
  "@context": "https://schema.org",
  "@type": "WebSite",
  "name": "L'Agence de Voglans",
  "url": "https://agencevoglans.fr",
  "description": "Agence immobilière 100% locale et humaine spécialisée dans la vente, l'achat et l'estimation de biens immobiliers à Voglans et en Savoie.",
  "potentialAction": {
    "@type": "SearchAction",
    "target": {
      "@type": "EntryPoint",
      "urlTemplate": "https://agencevoglans.fr/estimation/{search_term_string}"
    },
    "query-input": "required name=search_term_string"
  }
};

const organizationSchema = {
  "@context": "https://schema.org",
  "@type": "RealEstateAgent",
  "name": "L'Agence de Voglans",
  "alternateName": "Agence de Voglans",
  "url": "https://agencevoglans.fr",
  "logo": "https://agencevoglans.fr/images/logo-agence-voglans.png",
  "description": "Agence immobilière 100% locale et humaine à Voglans, Savoie. Expertise en vente, achat et estimation immobilière.",
  "telephone": "+33757830262",
  "email": "contact@agencevoglans.fr",
  "address": {
    "@type": "PostalAddress",
    "streetAddress": "Voglans",
    "addressLocality": "Voglans",
    "postalCode": "73420",
    "addressRegion": "Savoie",
    "addressCountry": "FR"
  },
  "geo": {
    "@type": "GeoCoordinates",
    "latitude": 45.6167,
    "longitude": 5.8833
  },
  "areaServed": [
    {
      "@type": "City",
      "name": "Voglans"
    },
    {
      "@type": "City",
      "name": "Chambéry"
    },
    {
      "@type": "City",
      "name": "Aix-les-Bains"
    },
    {
      "@type": "AdministrativeArea",
      "name": "Savoie"
    }
  ],
  "sameAs": [
    "https://agencevoglans.fr"
  ]
};
---

<Base title="L'Agence de Voglans | Immobilier d'Exception en Savoie">
  <Fragment slot="head">
    <script type="application/ld+json" set:html={JSON.stringify(websiteSchema)} />
    <script type="application/ld+json" set:html={JSON.stringify(organizationSchema)} />
  </Fragment>

  <!-- Hero Section -->
  <section class="relative min-h-screen flex items-center justify-center overflow-hidden bg-gradient-to-br from-emerald-400 via-emerald-600 to-emerald-700">
    <!-- Fond animé avec particules -->
    <div class="absolute inset-0">
      <div class="absolute top-1/4 left-1/4 w-96 h-96 bg-white/10 rounded-full blur-3xl animate-pulse"></div>
      <div class="absolute bottom-1/4 right-1/4 w-80 h-80 bg-white/5 rounded-full blur-2xl animate-pulse delay-1000"></div>
      <div class="absolute top-1/2 left-1/2 transform -translate-x-1/2 -translate-y-1/2 w-[600px] h-[600px] border border-white/20 rounded-full animate-spin opacity-20" style="animation-duration: 20s;"></div>
    </div>

    <!-- Grille de fond géométrique -->
    <div class="absolute inset-0 opacity-10">
      <svg width="100%" height="100%" class="absolute inset-0">
        <defs>
          <pattern id="grid" width="50" height="50" patternUnits="userSpaceOnUse">
            <path d="M 50 0 L 0 0 0 50" fill="none" stroke="white" stroke-width="1"/>
          </pattern>
        </defs>
        <rect width="100%" height="100%" fill="url(#grid)" />
      </svg>
    </div>

    <div class="container mx-auto px-4 relative z-10 text-center">
      <div class="max-w-5xl mx-auto">
        <!-- Badge flottant -->
        <div class="inline-flex items-center gap-3 bg-white/20 backdrop-blur-xl border border-white/30 text-white px-6 py-3 rounded-full text-sm font-semibold shadow-2xl mb-12 animate-float">
          <Sparkles className="w-4 h-4" />
          <span>100% en Ligne</span>
        </div>

        <!-- Titre principal -->
        <h1 class="text-6xl md:text-8xl font-black mb-8 leading-none">
          <span class="block text-white">
            L'Agence de Voglans
          </span>
        </h1>

        <!-- Baseline -->
        <p class="text-2xl md:text-3xl text-white font-light mb-12 max-w-4xl mx-auto leading-relaxed">
          L'immobilier 100% Local à destination des particuliers & professionnels !
        </p>

        <!-- CTA -->
        <div class="flex flex-col sm:flex-row gap-6 justify-center items-center">
          <a href="/estimation" class="group relative bg-white text-emerald-400 px-8 py-4 rounded-2xl font-bold text-lg hover:shadow-2xl hover:shadow-white/25 transition-all duration-300 inline-flex items-center gap-3 hover:scale-105">
            <Calculator className="w-5 h-5 group-hover:animate-bounce" />
            <span>Estimation Gratuite</span>
          </a>
          <a href={`tel:${phoneLink}`} class="group relative bg-white/20 backdrop-blur-xl border border-white/30 text-white px-8 py-4 rounded-2xl font-bold text-lg hover:shadow-2xl hover:shadow-white/25 transition-all duration-300 inline-flex items-center gap-3 hover:scale-105">
            <Phone className="w-5 h-5 group-hover:rotate-12 transition-transform duration-300" />
            <span>Nous Appeler</span>
          </a>
        </div>
      </div>
    </div>

    <!-- Indicateur de scroll -->
    <div class="absolute bottom-8 left-1/2 transform -translate-x-1/2 animate-bounce">
      <div class="w-6 h-10 border-2 border-white/50 rounded-full flex justify-center">
        <div class="w-1 h-3 bg-white rounded-full mt-2 animate-pulse"></div>
      </div>
    </div>
  </section>

  <!-- Stats Section -->
  <section class="py-32 bg-white relative overflow-hidden">
    <div class="absolute top-0 left-0 w-96 h-96 bg-emerald-400/10 rounded-full blur-3xl"></div>
    <div class="absolute bottom-0 right-0 w-80 h-80 bg-emerald-400/5 rounded-full blur-2xl"></div>

    <div class="container mx-auto px-4 relative z-10">
      <div class="max-w-6xl mx-auto">
        <div class="grid grid-cols-2 md:grid-cols-4 gap-8">
          <div class="text-center group">
            <div class="text-5xl md:text-6xl font-black text-emerald-300 mb-4 group-hover:scale-110 transition-transform duration-500">2025</div>
            <div class="text-sm font-semibold text-emerald-200 uppercase tracking-wider">CREATION</div>
          </div>
          <div class="text-center group">
            <div class="text-5xl md:text-6xl font-black text-emerald-300 mb-4 group-hover:scale-110 transition-transform duration-500">100%</div>
            <div class="text-sm font-semibold text-emerald-200 uppercase tracking-wider">Processus digital</div>
          </div>
          <div class="text-center group">
            <div class="text-5xl md:text-6xl font-black text-emerald-300 mb-4 group-hover:scale-110 transition-transform duration-500">Local</div>
            <div class="text-sm font-semibold text-emerald-200 uppercase tracking-wider">Expertise Savoie</div>
          </div>
          <div class="text-center group">
            <div class="text-5xl md:text-6xl font-black text-emerald-300 mb-4 group-hover:scale-110 transition-transform duration-500">24h</div>
            <div class="text-sm font-semibold text-emerald-200 uppercase tracking-wider">Réponse immédiate</div>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- Services Section -->
  <section class="py-32 bg-gradient-to-br from-emerald-400 via-emerald-600 to-emerald-700 relative overflow-hidden">
    <div class="container mx-auto px-4 relative z-10">
      <div class="max-w-7xl mx-auto">
        <div class="text-center mb-20">
          <div class="inline-flex items-center gap-3 bg-emerald-400/10 backdrop-blur-sm border border-emerald-400/20 text-white px-6 py-3 rounded-full text-sm font-semibold shadow-xl mb-8">
            <Award className="w-4 h-4" />
            <span>Nos services d'excellence</span>
          </div>
          <h2 class="text-5xl md:text-7xl font-black mb-8 text-white">
            Services <span class="text-white">100% Digital</span>
          </h2>
        </div>

        <div class="grid grid-cols-1 md:grid-cols-3 gap-12">
          <!-- Vendre -->
          <div class="group relative">
            <div class="absolute -inset-1 bg-gradient-to-r from-emerald-400 to-emerald-600 rounded-3xl blur opacity-25 group-hover:opacity-75 transition duration-1000"></div>
            <div class="relative bg-white rounded-3xl p-8 shadow-2xl border border-emerald-400/10">
              <div class="w-20 h-20 bg-gradient-to-br from-emerald-400 to-emerald-600 rounded-2xl flex items-center justify-center mx-auto mb-6 group-hover:scale-110 transition-transform duration-500">
                <Home className="w-10 h-10 text-white" />
              </div>
              <h3 class="text-3xl font-bold mb-6 text-center group-hover:text-emerald-400 transition-colors duration-300">Vendre</h3>
              <p class="text-muted leading-relaxed text-center mb-8">
                Accompagnement d'exception avec estimation gratuite et marketing premium pour votre bien immobilier.
              </p>
              <div class="space-y-3 mb-8">
                <div class="flex items-center gap-3">
                  <CheckCircle className="w-5 h-5 text-emerald-400" />
                  <span class="text-sm">Estimation précise sous 24h</span>
                </div>
                <div class="flex items-center gap-3">
                  <CheckCircle className="w-5 h-5 text-emerald-400" />
                  <span class="text-sm">Visite virtuelle 360°</span>
                </div>
                <div class="flex items-center gap-3">
                  <CheckCircle className="w-5 h-5 text-emerald-400" />
                  <span class="text-sm">Marketing digital avancé</span>
                </div>
              </div>
              <a href="/vendre" class="w-full bg-gradient-to-r from-emerald-400 to-emerald-600 text-white py-4 rounded-2xl font-bold hover:shadow-xl hover:scale-105 transition-all duration-300 text-center block">
                Découvrir nos méthodes →
              </a>
            </div>
          </div>

          <!-- Acheter -->
          <div class="group relative">
            <div class="absolute -inset-1 bg-gradient-to-r from-emerald-600 to-emerald-700 rounded-3xl blur opacity-25 group-hover:opacity-75 transition duration-1000"></div>
            <div class="relative bg-white rounded-3xl p-8 shadow-2xl border border-emerald-400/10">
              <div class="w-20 h-20 bg-gradient-to-br from-emerald-600 to-emerald-700 rounded-2xl flex items-center justify-center mx-auto mb-6 group-hover:scale-110 transition-transform duration-500">
                <Building className="w-10 h-10 text-white" />
              </div>
              <h3 class="text-3xl font-bold mb-6 text-center group-hover:text-emerald-400 transition-colors duration-300">Acheter</h3>
              <p class="text-muted leading-relaxed text-center mb-8">
                Recherche sur-mesure et accompagnement expert pour trouver le bien idéal en Savoie.
              </p>
              <div class="space-y-3 mb-8">
                <div class="flex items-center gap-3">
                  <CheckCircle className="w-5 h-5 text-emerald-400" />
                  <span class="text-sm">Recherche personnalisée</span>
                </div>
                <div class="flex items-center gap-3">
                  <CheckCircle className="w-5 h-5 text-emerald-400" />
                  <span class="text-sm">Visites virtuelles</span>
                </div>
                <div class="flex items-center gap-3">
                  <CheckCircle className="w-5 h-5 text-emerald-400" />
                  <span class="text-sm">Accompagnement juridique</span>
                </div>
              </div>
              <a href="/acheter" class="w-full bg-gradient-to-r from-emerald-600 to-emerald-700 text-white py-4 rounded-2xl font-bold hover:shadow-xl hover:scale-105 transition-all duration-300 text-center block">
                Trouver mon bien →
              </a>
            </div>
          </div>

          <!-- Estimer -->
          <div class="group relative">
            <div class="absolute -inset-1 bg-gradient-to-r from-emerald-500 to-emerald-600 rounded-3xl blur opacity-25 group-hover:opacity-75 transition duration-1000"></div>
            <div class="relative bg-white rounded-3xl p-8 shadow-2xl border border-emerald-400/10">
              <div class="w-20 h-20 bg-gradient-to-br from-emerald-500 to-emerald-600 rounded-2xl flex items-center justify-center mx-auto mb-6 group-hover:scale-110 transition-transform duration-500">
                <Calculator className="w-10 h-10 text-white" />
              </div>
              <h3 class="text-3xl font-bold mb-6 text-center group-hover:text-emerald-400 transition-colors duration-300">Estimer</h3>
              <p class="text-muted leading-relaxed text-center mb-8">
                Estimation gratuite et précise de votre bien immobilier en moins de 24 heures.
              </p>
              <div class="space-y-3 mb-8">
                <div class="flex items-center gap-3">
                  <CheckCircle className="w-5 h-5 text-emerald-400" />
                  <span class="text-sm">Analyse de marché locale</span>
                </div>
                <div class="flex items-center gap-3">
                  <CheckCircle className="w-5 h-5 text-emerald-400" />
                  <span class="text-sm">Rapport détaillé gratuit</span>
                </div>
                <div class="flex items-center gap-3">
                  <CheckCircle className="w-5 h-5 text-emerald-400" />
                  <span class="text-sm">Expertise personnalisée</span>
                </div>
              </div>
              <a href="/estimation" class="w-full bg-gradient-to-r from-emerald-500 to-emerald-600 text-white py-4 rounded-2xl font-bold hover:shadow-xl hover:scale-105 transition-all duration-300 text-center block">
                Estimer gratuitement →
              </a>
            </div>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- Zone d'Intervention Section -->
  <section class="py-32 bg-white relative overflow-hidden">
    <div class="absolute inset-0 opacity-20">
      <div class="absolute top-20 left-20 w-4 h-4 bg-black rounded-full animate-float"></div>
      <div class="absolute top-40 right-40 w-6 h-6 bg-black rounded-full animate-float delay-1000"></div>
      <div class="absolute bottom-40 left-40 w-3 h-3 bg-black rounded-full animate-float delay-500"></div>
      <div class="absolute bottom-20 right-20 w-5 h-5 bg-black rounded-full animate-float delay-1500"></div>
    </div>

    <div class="container mx-auto px-4 relative z-10">
      <div class="max-w-7xl mx-auto">
        <div class="text-center mb-20">
          <h2 class="text-5xl md:text-7xl font-black mb-8 text-black">
            Notre <span class="text-black">Territoire</span>
          </h2>
          <p class="text-xl text-black max-w-3xl mx-auto">
            Expertise complète sur l'ensemble du territoire savoyard
          </p>
        </div>

        <div class="grid grid-cols-1 lg:grid-cols-2 gap-16 items-center">
          <!-- Vidéo de zone d'intervention -->
          <div class="relative">
            <div class="relative rounded-3xl overflow-hidden shadow-2xl bg-gradient-to-br from-black/20 to-black/10 border-2 border-black/20">
              <video
                autoplay
                muted
                loop
                playsinline
                class="w-full h-80 lg:h-96 object-cover"
                poster="/images/zone-intervention-poster.jpg"
              >
                <source src="/videos/zone.mp4" type="video/mp4">
                <div class="w-full h-80 lg:h-96 bg-gradient-to-br from-black to-black/60 flex items-center justify-center">
                  <div class="text-center text-black">
                    <MapPin className="w-16 h-16 mx-auto mb-4" />
                    <p class="text-sm opacity-80">Voglans • Chambéry • Le Bourget-du-Lac</p>
                  </div>
                </div>
              </video>

              <div class="absolute bottom-0 left-0 right-0 bg-gradient-to-t from-white/90 via-white/70 to-transparent p-6">
                <div class="text-black">
                  <div class="flex items-center gap-4 text-xs font-bold">
                    <span class="flex items-center gap-1 text-black">
                      <div class="w-1 h-1 bg-emerald-600 rounded-full"></div>
                      Expertise locale
                    </span>
                    <span class="flex items-center gap-1 text-black">
                      <div class="w-1 h-1 bg-emerald-600 rounded-full"></div>
                      Savoir-faire artisanal
                    </span>
                    <span class="flex items-center gap-1 text-black">
                      <div class="w-1 h-1 bg-emerald-600 rounded-full"></div>
                      Service d'exception
                    </span>
                  </div>
                </div>
              </div>
            </div>

            <div class="absolute -top-8 -left-8 w-16 h-16 bg-black/20 rounded-full opacity-60"></div>
            <div class="absolute -bottom-8 -right-8 w-24 h-24 bg-black/10 rounded-full blur-xl"></div>
          </div>

          <!-- Logo et informations -->
          <div class="text-center lg:text-left">
            <div class="relative">
              <div class="w-80 h-80 mx-auto lg:mx-0 bg-gradient-to-br from-black/20 to-black/10 backdrop-blur-sm rounded-full flex items-center justify-center border-2 border-black/30 shadow-2xl">
                <div class="w-64 h-64 bg-white/90 backdrop-blur-sm rounded-full flex items-center justify-center border border-black/20">
                  <Logo size="xl" className="text-black w-32 h-32" />
                </div>
              </div>

              <div class="mt-12 space-y-6">
                <h3 class="text-3xl font-bold text-white">L'Agence de Voglans</h3>
                <p class="text-muted leading-relaxed max-w-md mx-auto lg:mx-0">
                  Née au cœur de la Savoie, notre agence incarne l'excellence et le raffinement de l'immobilier local.
                </p>
                <div class="flex items-center justify-center lg:justify-start gap-6 text-sm">
                  <span class="flex items-center gap-2 text-black font-semibold">
                    <div class="w-2 h-2 bg-black rounded-full animate-pulse"></div>
                    100% Locale
                  </span>
                  <span class="flex items-center gap-2 text-black font-semibold">
                    <div class="w-2 h-2 bg-black rounded-full animate-pulse"></div>
                    100% Humaine
                  </span>
                </div>
              </div>
            </div>
          </div>
        </div>

        <!-- Statistiques -->
        <div class="mt-20 grid grid-cols-1 md:grid-cols-3 gap-8 max-w-4xl mx-auto">
          <div class="text-center group">
            <div class="text-4xl font-black text-black mb-3 group-hover:scale-110 transition-transform duration-500">
              <span class="count" data-target="15">15</span><span class="align-top text-lg">km</span>
            </div>
            <div class="text-sm font-semibold text-black uppercase tracking-wider">Rayon d'Excellence</div>
          </div>
          <div class="text-center group">
            <div class="text-4xl font-black text-black mb-3 group-hover:scale-110 transition-transform duration-500">
              <span class="count" data-target="24">24</span><span class="align-top text-lg">h</span>
            </div>
            <div class="text-sm font-semibold text-black uppercase tracking-wider">Réactivité Premium</div>
          </div>
          <div class="text-center group">
            <div class="text-4xl font-black text-black mb-3 group-hover:scale-110 transition-transform duration-500">
              <TrendingUp className="w-16 h-16 mx-auto" />
            </div>
            <div class="text-sm font-semibold text-black uppercase tracking-wider">Made in Savoie</div>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- CTA Final Section -->
  <section class="py-32 bg-gradient-to-br from-emerald-400 via-emerald-600 to-emerald-700 relative overflow-hidden">
    <div class="absolute inset-0">
      <div class="absolute top-0 left-0 w-full h-full bg-black/10"></div>
      <div class="absolute top-1/4 left-1/4 w-96 h-96 bg-white/10 rounded-full blur-3xl animate-pulse"></div>
      <div class="absolute bottom-1/4 right-1/4 w-80 h-80 bg-white/5 rounded-full blur-2xl animate-pulse delay-1000"></div>
    </div>

    <div class="container mx-auto px-4 relative z-10">
      <div class="max-w-5xl mx-auto text-center">
        <div class="inline-flex items-center gap-3 bg-white/20 backdrop-blur-xl border border-white/30 text-white px-8 py-4 rounded-full text-lg font-semibold shadow-2xl mb-12">
          <Sparkles className="w-6 h-6" />
          <span>Prêt à Révolutionner Votre Projet ?</span>
        </div>

        <h2 class="text-6xl md:text-8xl font-black mb-8 leading-none text-white">
          Contactez-nous dès maintenant !
        </h2>

        <p class="text-2xl text-white mb-12 max-w-4xl mx-auto leading-relaxed">
          Découvrez comment notre approche 100% digitale et notre expertise locale
          peuvent transformer votre expérience immobilière dès aujourd'hui.
        </p>

        <div class="flex flex-col sm:flex-row gap-8 justify-center items-center mb-12">
          <a
            href={`tel:${phoneLink}`}
            class="group relative bg-white text-emerald-400 px-10 py-5 rounded-2xl font-bold text-xl hover:shadow-2xl hover:shadow-white/25 transition-all duration-300 inline-flex items-center justify-center gap-4 hover:scale-105"
          >
            <Phone className="w-6 h-6 group-hover:rotate-12 transition-transform duration-300" />
            <span>{siteConfig.contact.phone}</span>
          </a>
          <a href="/contact" class="group relative bg-white/20 backdrop-blur-xl border border-white/30 text-white px-10 py-5 rounded-2xl font-bold text-xl hover:shadow-2xl hover:shadow-white/25 transition-all duration-300 inline-flex items-center justify-center gap-4 hover:scale-105">
            <span>Nous rencontrer</span>
            <ArrowRight className="w-6 h-6 group-hover:translate-x-1 transition-transform duration-300" />
          </a>
        </div>

        <div class="text-white/80 text-lg">
          <div class="inline-flex items-center gap-3">
            <div class="w-3 h-3 bg-white rounded-full animate-pulse"></div>
            <span>93 chemin de la Combe, 73420 Voglans</span>
          </div>
        </div>
      </div>
    </div>
  </section>
</Base>

<style>
  .delay-500 { animation-delay: 0.5s; }
  .delay-1000 { animation-delay: 1s; }
  .delay-1500 { animation-delay: 1.5s; }
</style>

<script>
  const counters = document.querySelectorAll('.count');

  function animateCounter(el: HTMLElement) {
    const target = Number(el.getAttribute('data-target')) || 0;
    const duration = 1200;
    const startTime = performance.now();

    function tick(now: number) {
      const progress = Math.min((now - startTime) / duration, 1);
      const value = Math.floor(progress * target);
      el.textContent = String(value);
      if (progress < 1) requestAnimationFrame(tick);
    }
    requestAnimationFrame(tick);
  }

  const observer = new IntersectionObserver((entries, obs) => {
    entries.forEach(entry => {
      if (entry.isIntersecting) {
        counters.forEach(c => {
          const element = c as HTMLElement;
          if (!element.dataset.done) {
            animateCounter(element);
            element.dataset.done = '1';
          }
        });
        obs.disconnect();
      }
    });
  }, { threshold: 0.3 });

  const firstCounter = document.querySelector('.count') as HTMLElement;
  if (firstCounter) observer.observe(firstCounter);
</script>
