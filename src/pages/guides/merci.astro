---
import Base from '@/layouts/Base.astro';

const guide = Astro.url.searchParams.get('guide') || 'vendre-2025';
const email = Astro.url.searchParams.get('email') || '';

const guides = {
  'vendre-2025': {
    title: 'Guide Complet de la Vente Immobilière 2025',
    pdf: '/guides/vendre-2025.pdf',
    pages: 42
  },
  'diagnostics': {
    title: 'Checklist des Diagnostics Obligatoires',
    pdf: '/guides/diagnostics-obligatoires.pdf',
    pages: 28
  },
  'estimation': {
    title: 'Comment Estimer Son Bien',
    pdf: '/guides/estimation-bien.pdf',
    pages: 35
  },
  'fiscalite': {
    title: 'Fiscalité de la Vente Immobilière',
    pdf: '/guides/fiscalite-vente.pdf',
    pages: 32
  },
  'preparer': {
    title: 'Préparer Sa Maison Pour la Vente',
    pdf: '/guides/preparer-maison.pdf',
    pages: 25
  }
};

const currentGuide = guides[guide] || guides['vendre-2025'];
---

<Base title="Merci ! Votre guide est prêt | L'Agence de Voglans">
  <section class="min-h-screen bg-gradient-to-br from-primary/5 to-white flex items-center justify-center py-20">
    <div class="container mx-auto px-4">
      <div class="max-w-2xl mx-auto">
        
        <!-- Success Message -->
        <div class="text-center mb-8">
          <div class="inline-flex items-center justify-center w-20 h-20 bg-green-100 rounded-full mb-6">
            <svg class="w-12 h-12 text-green-600" fill="currentColor" viewBox="0 0 20 20">
              <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.707-9.293a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z" clip-rule="evenodd" />
            </svg>
          </div>
          <h1 class="text-4xl font-bold text-gray-900 mb-4">
            C'est dans votre boîte mail ! 📧
          </h1>
          <p class="text-xl text-gray-600">
            Votre guide "{currentGuide.title}" vient d'être envoyé à <strong>{email}</strong>
          </p>
        </div>

        <!-- Download Card -->
        <div class="bg-white rounded-2xl shadow-xl p-8 mb-8">
          <div class="flex items-center gap-4 mb-6">
            <div class="flex-shrink-0 w-16 h-16 bg-primary/10 rounded-lg flex items-center justify-center">
              <svg class="w-8 h-8 text-primary" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 10v6m0 0l-3-3m3 3l3-3m2 8H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z" />
              </svg>
            </div>
            <div class="flex-1">
              <h2 class="text-xl font-bold text-gray-900">{currentGuide.title}</h2>
              <p class="text-sm text-gray-600">{currentGuide.pages} pages • Format PDF</p>
            </div>
          </div>

          <a 
            href={currentGuide.pdf}
            download
            class="flex w-full bg-primary text-white text-center px-6 py-4 rounded-lg font-bold hover:bg-primary-dark transition items-center justify-center gap-2"
          >
            <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 10v6m0 0l-3-3m3 3l3-3m2 8H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z" />
            </svg>
            Télécharger le PDF maintenant
          </a>

          <div class="mt-6 pt-6 border-t border-gray-200">
            <div class="flex items-start gap-3">
              <svg class="w-5 h-5 text-primary flex-shrink-0 mt-1" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 8l7.89 5.26a2 2 0 002.22 0L21 8M5 19h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z" />
              </svg>
              <div class="text-sm text-gray-600">
                <p class="font-semibold text-gray-900 mb-1">Vous ne l'avez pas reçu ?</p>
                <p>Vérifiez vos spams ou contactez-nous au <a href="tel:+33757830262" class="text-primary hover:underline font-semibold">07 57 83 02 62</a></p>
              </div>
            </div>
          </div>
        </div>

        <!-- Autres guides -->
        <div class="bg-gradient-to-r from-primary/5 to-primary/10 rounded-xl p-6 mb-8">
          <h3 class="text-lg font-bold text-gray-900 mb-4">📚 Nos autres guides gratuits :</h3>
          <div class="space-y-3">
            {Object.entries(guides).filter(([key]) => key !== guide).slice(0, 3).map(([key, g]) => (
              <a href={`/guides/${key}`} class="flex items-center justify-between p-3 bg-white rounded-lg hover:shadow-md transition">
                <span class="text-gray-900 font-medium">{g.title}</span>
                <svg class="w-5 h-5 text-primary" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7" />
                </svg>
              </a>
            ))}
          </div>
        </div>

        <!-- CTA -->
        <div class="text-center">
          <p class="text-gray-600 mb-4">Besoin d'un accompagnement personnalisé ?</p>
          <a href="/contact" class="inline-flex items-center gap-2 bg-white text-primary border-2 border-primary px-6 py-3 rounded-lg font-bold hover:bg-primary hover:text-white transition">
            Prendre rendez-vous
            <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7" />
            </svg>
          </a>
        </div>

      </div>
    </div>
  </section>
</Base>
