---
import type { Commune } from '../../data/communes';

interface Props {
  name: string;
  communesProches: string[];
  allCommunes: Commune[];
}

const { name, communesProches, allCommunes } = Astro.props;

// Récupérer les noms des communes proches
const communesProchesNames = communesProches
  .map(slug => allCommunes.find(c => c.slug === slug)?.name)
  .filter(Boolean)
  .slice(0, 3)
  .join(', ');

const faqs = [
  {
    question: `Combien de temps pour obtenir une estimation à ${name} ?`,
    answer: `Nous vous remettons votre estimation sous 48h maximum après la visite de votre bien à ${name}. En cas d'urgence, nous pouvons accélérer le processus.`
  },
  {
    question: "Est-ce payant ?",
    answer: "Non, l'estimation de votre bien est entièrement gratuite et sans engagement. Vous ne payez que si vous décidez de nous confier la vente de votre bien."
  },
  {
    question: "Puis-je vendre au-dessus de l'estimation ?",
    answer: `Oui, le prix de vente final est votre décision. Nous vous conseillons une fourchette réaliste basée sur le marché de ${name}, mais vous restez libre de fixer le prix que vous souhaitez.`
  },
  {
    question: "Comment est calculée l'estimation ?",
    answer: `Nous croisons plusieurs données : les ventes récentes à ${name}, les caractéristiques de votre bien (surface, état, exposition), l'environnement (écoles, transports, commerces) et les tendances actuelles du marché.`
  },
  {
    question: `Vous intervenez aussi dans les communes proches ?`,
    answer: `Oui, nous intervenons dans toute la région : ${communesProchesNames} et bien d'autres communes de Savoie. Consultez notre <a href="/zone-intervention" class="text-primary hover:underline font-semibold">zone d'intervention complète</a>.`
  }
];
---

<section class="py-16 bg-gradient-to-br from-white to-primary/5">
  <div class="container mx-auto px-4">
    <div class="max-w-4xl mx-auto">
      <div class="text-center mb-12">
        <h2 class="text-3xl md:text-4xl font-semibold mb-4 text-foreground">
          Questions fréquentes
        </h2>
        <p class="text-xl text-muted">
          Tout ce que vous devez savoir sur l'estimation à {name}
        </p>
      </div>

      <div class="space-y-4">
        {faqs.map((faq, index) => (
          <details class="group bg-white rounded-xl shadow-md hover:shadow-lg transition-all duration-300">
            <summary class="flex items-center justify-between p-6 cursor-pointer list-none">
              <h3 class="text-lg font-semibold text-foreground pr-4">
                {faq.question}
              </h3>
              <svg 
                class="w-6 h-6 text-primary flex-shrink-0 transition-transform duration-300 group-open:rotate-180" 
                fill="none" 
                stroke="currentColor" 
                viewBox="0 0 24 24"
              >
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7"/>
              </svg>
            </summary>
            <div class="px-6 pb-6">
              <p class="text-muted leading-relaxed" set:html={faq.answer} />
            </div>
          </details>
        ))}
      </div>

      <!-- CTA après FAQ -->
      <div class="mt-12 text-center bg-white rounded-2xl p-8 shadow-lg">
        <h3 class="text-2xl font-semibold mb-4 text-foreground">
          Une autre question ?
        </h3>
        <p class="text-muted mb-6">
          Notre équipe est à votre écoute pour répondre à toutes vos questions sur l'estimation à {name}.
        </p>
        <div class="flex flex-col sm:flex-row gap-4 justify-center">
          <a 
            href="tel:0757830262" 
            class="btn-primary px-8 py-3 rounded-full inline-flex items-center justify-center gap-2"
          >
            <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 5a2 2 0 012-2h3.28a1 1 0 01.948.684l1.498 4.493a1 1 0 01-.502 1.21l-2.257 1.13a11.042 11.042 0 005.516 5.516l1.13-2.257a1 1 0 011.21-.502l4.493 1.498a1 1 0 01.684.949V19a2 2 0 01-2 2h-1C9.716 21 3 14.284 3 6V5z"/>
            </svg>
            07 57 83 02 62
          </a>
          <a 
            href="/contact" 
            class="btn-luxury-outline px-8 py-3 rounded-full inline-flex items-center justify-center gap-2"
          >
            <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 8l7.89 5.26a2 2 0 002.22 0L21 8M5 19h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z"/>
            </svg>
            Nous contacter
          </a>
        </div>
      </div>
    </div>
  </div>
</section>
